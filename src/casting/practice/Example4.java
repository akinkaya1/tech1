package casting.practice;

public class Example4 {
    public static void main(String[] args) {
        /*
        create a float variable that holds your balance which is 200
        create another variable that holds isTodaySnowy as true or false
        Convert those into string using valueOf() method
         */
        float balance = 200F;
        boolean isTodaySnowy = true;

        String balanceString = String.valueOf(balance);
        String isTodaySnowyAnswer = String.valueOf(isTodaySnowy);

    }
}
